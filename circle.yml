machine:
  php:
    verison: 5.6
  post:
    - sudo service postgresql stop

depensency:
  override:
#    - php composer.phar install # auto run

database:
  post:
    - mysql -u root < $HOME/$CIRCLE_PROJECT_REPONAME/test/db/setup.sql
    - mysql test -u root < $HOME/$CIRCLE_PROJECT_REPONAME/test/db/data.sql

test:
  override:
    - echo Add PHP
